# Range-Excluded Mutant Candidate: BoundaryStressCounter_M8

Status: validated exploratory candidate; not merged into the paper experiment tables.

## Mutation

- Subject: `BoundaryStressCounter`
- Mutant: `BoundaryStressCounter_M8`
- Operator: AOR, statement/data-flow fault
- Source line: 17
- Original update: `score = score + 5`
- Mutated update: `score = score + 5 + quota / 500`

The mutation preserves the control-flow structure. For the high-quota values
generated by the current CSC-only and Boundary runs (`202`, `205`, and `206`),
integer division makes `quota / 500 == 0`. The mutation becomes observable at
the valid but range-excluded input value `quota = 500`.

## Validation Results

| Check | Result |
|---|---:|
| CSC-only test records | 15 |
| CSC-only TBFV failures | 0 |
| Boundary test records (`range-bound=200`) | 81 |
| Boundary TBFV failures | 0 |
| Boundary `RANGE_EXCLUDED` leaves | 10 |
| Boundary tests containing `quota=500` | 0 |

Both generated suites therefore leave the mutant alive, while the Boundary CCT
explicitly records exploration stopped by the configured range bound.

## Killing Witness

Witness: `steps=3, quota=500, boost=0`.

This input belongs to `fsf_4`:

- `3` is within the normal `steps` range;
- `500` satisfies `quota > 200 && quota <= 500`;
- `boost=0` is within the normal boost range;
- the specified result is `5 * ((3 + 2) / 3) == 5` under Java integer division.

| Program | Return value | FSF result |
|---|---:|---|
| Original | 5 | pass |
| `BoundaryStressCounter_M8` | 6 | fail |

The witness proves that M8 is not equivalent relative to the FSF. Its survival
under `range-bound=200` is attributable to the missing high-end value within an
already covered condition path, rather than to an absent FSF scenario.

## Artifact Locations

- CSC-only report: `csc_only/artifacts/csc_tmp/range_excluded_m8_csc_only_BoundaryStressCounter_M8/BoundaryStressCounter/refined_tbfv_report.json`
- Boundary report: `boundary/artifacts/csc_tmp/range_excluded_m8_boundary_BoundaryStressCounter_M8/BoundaryStressCounter/refined_tbfv_report.json`
- Boundary CCT statistics: `boundary/artifacts/csc_tmp/range_excluded_m8_boundary_BoundaryStressCounter_M8/BoundaryStressCounter/cct_stats.json`
- Witness driver: `witness/RangeExcludedWitness.java`
