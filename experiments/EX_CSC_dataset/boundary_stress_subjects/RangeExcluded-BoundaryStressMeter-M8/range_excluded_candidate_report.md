# Range-Excluded Mutant: BoundaryStressMeter_M8

Status: validated for inclusion in `EX_CSC_dataset`; not yet merged into paper tables.

## Mutation

- Subject: `BoundaryStressMeter`
- Mutant: `BoundaryStressMeter_M8`
- Operator: AOR, statement/data-flow fault
- Source line: 17
- Original update: `charge = charge + 6`
- Mutated update: `charge = charge + 6 + limit / 500`

The mutation preserves the control-flow structure. The valid high-limit values
generated by the current suites are below 500, so Java integer division makes
the added term zero. It becomes observable at the valid input `limit=500`.

## Validation Results

| Check | Result |
|---|---:|
| CSC-only test records | 15 |
| CSC-only TBFV failures | 0 |
| Boundary test records (`range-bound=200`) | 81 |
| Boundary TBFV failures | 0 |
| Boundary `RANGE_EXCLUDED` leaves | 10 |
| Boundary tests containing `limit=500` | 0 |

## Killing Witness

Witness: `days=3, limit=500, surge=0`.

This input belongs to `fsf_4`, which requires
`return_value == 6 * ((3 + 2) / 3) == 6` under Java integer division.

| Program | Return value | FSF result |
|---|---:|---|
| Original | 6 | pass |
| `BoundaryStressMeter_M8` | 7 | fail |

The witness proves that M8 is not equivalent relative to the FSF. Its survival
under `range-bound=200` is attributable to bounded value exploration within an
already covered condition path.
