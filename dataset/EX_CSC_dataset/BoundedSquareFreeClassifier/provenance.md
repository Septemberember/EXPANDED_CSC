# BoundedSquareFreeClassifier Provenance

Adapted from TheAlgorithms/Java `maths/Prime/SquareFreeInteger.java`.

The dataset version replaces collection-based prime-factor storage with a full
bounded square-divisor scan that keeps branch-visible state instead of returning early.
