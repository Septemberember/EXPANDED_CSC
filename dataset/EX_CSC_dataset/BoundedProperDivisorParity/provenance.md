# BoundedProperDivisorParity Provenance

Adapted from TheAlgorithms/Java proper-divisor accumulation utilities, including
`AliquotSum` and `SociableNumber`.

The dataset version classifies the parity of the proper-divisor sum.

Validation adjustment: accepted range narrowed to `1 <= number <= 50` to keep batch CSC within budget.
