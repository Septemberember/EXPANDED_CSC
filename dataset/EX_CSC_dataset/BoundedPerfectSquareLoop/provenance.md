# BoundedPerfectSquareLoop Provenance

Adapted from TheAlgorithms/Java `maths/PerfectSquare.java` and
`maths/FibonacciNumberCheck.java` perfect-square helper behavior.

The dataset version uses a bounded scalar search loop instead of library square-root calls.
