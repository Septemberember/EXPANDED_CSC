# BoundedDivisorCountClassifier Provenance

Adapted from TheAlgorithms/Java divisor-loop number-theory utilities such as
`PerfectNumber`, `AbundantNumber`, and `PrimeCheck`.

The dataset version classifies numbers by divisor-count density.

Validation adjustment: accepted range narrowed to `1 <= number <= 50` to keep batch CSC within budget.
