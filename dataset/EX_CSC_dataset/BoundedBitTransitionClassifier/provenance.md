# BoundedBitTransitionClassifier Provenance

Adapted from TheAlgorithms/Java bit-manipulation counting utilities such as
`ParityCheck`, `CountSetBits`, and binary representation examples.

The dataset version scans a fixed-width 7-bit representation and classifies the
number of adjacent bit transitions.
