# BoundedPopcountDensity Provenance

Adapted from TheAlgorithms/Java `bitmanipulation/CountSetBits.java` and related
bit-counting utilities.

The dataset version counts set bits with a scalar division/modulo loop and
classifies the density into low, medium, or high.
