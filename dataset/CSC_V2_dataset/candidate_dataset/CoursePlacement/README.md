# CoursePlacement

Main benchmark candidate with larger source-line space but controlled branching.

- Domain: course placement recommendation.
- Shape: validation guards, mutually exclusive score bands, and weighted
  post-processing statements.
- Return categories: `-1` invalid, `1` remedial, `2` standard, `3` advanced,
  `4` honors.

